# Platform-specific configuration for Windows
target_platform=win32
target_arch=x64
target_libc=unknown

# Prefer prebuilt binaries over building from source
prefer-offline=true
cache-max=86400000

# Node-gyp configuration for Windows
python=C:\Python311\python.exe
msvs_version=2022

# Force npm to use prebuilt binaries when available
prefer-offline=true